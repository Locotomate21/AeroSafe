# AeroSafe - Dependencias de desarrollo y testing
# Instalar: pip install -r requirements-dev.txt

-r requirements.txt

pytest==9.0.2
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Cliente de test para endpoints FastAPI (TestClient depende de httpx)
anyio==4.11.0
